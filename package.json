{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev ./packages/cli/src/cli.ts",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "@types/dotenv-safe": "^8.1.2",
    "@types/node": "^17.0.18",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "dotenv-safe": "^8.2.0"
  }
}
